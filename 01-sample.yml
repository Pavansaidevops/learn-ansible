- name: Install Nginx
  hosts: WEB
  become: true
  tasks:
    - name: install nginx
      ansible.builtin.yum:
        name: httpd
        state: latest

    - name: Start Nginx
      ansible.builtin.systemd_service:
        name: httpd
        state: started